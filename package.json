{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "CORS Proxy for Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production"
  },
  "keywords": [
    "cors",
    "proxy",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}